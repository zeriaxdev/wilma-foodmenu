version: "2"

services:
  foodmenu:
    build:
      context: .
    image: foodmenu_server
    shm_size: '2gb'
    environment:
      SKIP_SELENIUM_QUIT: ${SKIP_SELENIUM_QUIT}
    ports:
      - "3000:3000"
    restart: unless-stopped